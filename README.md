# AI 세무사

상담 자리에서 고객의 예상 세금을 개산하는 단일 페이지 웹 도구.
상속·증여·양도·법인 8개 세목을 5단계 위저드로 순회하고, 결과에 계산 과정과 적용 기준을 함께 낸다.

**바로 사용 → https://sexyback83.github.io/tax-review/**

설치도 서버도 필요 없다. 내려받아 `company_tax/tax-review/index.html` 을 브라우저로 열어도 똑같이 동작한다.

## 다루는 세목

| 세목 | 주요 내용 |
|---|---|
| 상속세 | 일괄/인적공제 자동 유리선택 · 배우자상속공제 · 금융재산공제 · 1·2차 상속 합계 |
| 증여세 | 부담부증여 — 증여분과 양도분을 나눠 계산 |
| 종합부동산세 | 1세대1주택 기본공제 · 고령자·장기보유 세액공제 · 농어촌특별세 |
| 양도소득세 | 1세대1주택 비과세 안분 · 장기보유특별공제 표1·표2 · 다주택 중과 |
| 비상장주식 평가 | 순자산·순손익 가중평균 · 순자산가치 하한 · 최대주주 할증 |
| 급여 및 배당 | 금융소득종합과세 · 4대보험 · 급여 증감 비교 |
| 임원퇴직금 | 소득세법 제22조 제3항 세 구간 한도 · 한도 초과분 근로소득 전환 |
| 가업승계 | 가업상속공제 · 증여세 과세특례 비교 |

현행 기준이 기본이고, 2026 세제개편안을 적용 연도(2027·2028·2029)별로 비교할 수 있다.

## 설계 원칙

- **계산과 화면의 분리** — 세율·공제 상수와 산식은 `calc.js` 에만 둔다. 화면 코드에 법정 수치를 두지 않는다.
- **저장하지 않고 전송하지 않는다** — `localStorage`·쿠키·`fetch` 를 쓰지 않는다. 입력값은 브라우저를 벗어나지 않고, 위치 정보는 거리 계산에만 쓰고 즉시 버린다.
- **프레임워크·빌드도구 없음** — 의존성이 0개다. Node.js 는 테스트 실행에만 쓴다.
- **기대값은 법령에서 전개한다** — 테스트 기대값을 구현을 실행해서 얻지 않고, 조문과 국세청 공표 세율표에서 손으로 전개해 근거를 주석으로 남긴다.

## 검증

`company_tax/tax-review/` 에서 실행한다. 테스트 프레임워크를 쓰지 않으므로 설치가 필요 없다(pytest만 예외).

```bash
node calc.test.js              # 회귀 124건 — 구현이 설계대로인가
node audit.test.js             # 법령 대조 감사 36건 — 설계가 법대로인가
node fp.test.js                # FP센터 데이터·거리 253개 단언
node oracle/calc.test.js       # 독립 재구현 26건
node oracle/fixture-check.js   # 언어 중립 fixture 43건
node oracle/crosscheck-run.js  # JS↔Python 무작위 차분 400케이스 2,400값
python -m pytest oracle/test_calc.py
```

`audit.test.js` 는 구현·기존 테스트·문서를 참조하지 않고 법 조문에서만 기대값을 전개한다.
소유자가 구현하지 않기로 한 항목은 「범위 제외」로 따로 세고, **그 항목이 통과하기 시작하면 결함으로 보고**한다.

## 구조

```
company_tax/tax-review/
  index.html     화면 전부 (인라인 CSS·JS)
  calc.js        세액 계산 순수 함수와 법정 상수 — 조문 주석 포함
  fp.js          FP센터 데이터·하버사인 거리·지도 투영
  *.test.js      회귀·감사·FP 테스트
  oracle/        JS·Python 독립 재구현 교차검증
.claude/skills/tax-review/
  SKILL.md       Claude Code 스킬 정의
  scripts/tax.js 세액을 명령줄에서 바로 계산하는 CLI
```

`AI세무사_에이전트_재생성_프롬프트.md` 하나만 있으면 이 도구를 처음부터 다시 만들 수 있다.
제약·화면 불변조건·법령 근거·자기검증 벡터가 전부 들어 있다.

명령줄에서 세액을 계산하려면:

```bash
node .claude/skills/tax-review/scripts/tax.js inherit --re 200000 --cash 100000 --children 2 --second
node .claude/skills/tax-review/scripts/tax.js selftest
```

금액 단위는 화면과 같은 만원이다 (20억 → `200000`).

## 배포

공개 사이트(https://sexyback83.github.io/tax-review/)는 `gh-pages` 브랜치의 `index.html` 한 개를 서빙한다.
그 파일은 `build-standalone.js` 가 원본을 자기완결 형태로 묶은 것이다 — `calc.js`·`fp.js` 인라인, 이미지 data URI 화.

```bash
node build-standalone.js
```

**앱의 빌드도구가 아니다.** `company_tax/tax-review/index.html` 은 이 스크립트 없이도 브라우저로 열면 그대로 동작한다.
이 스크립트는 배포본을 만들 뿐이며, 문서 껍데기(`doctype`·`head`·`body`)를 건드리지 않는다 —
`<meta name="viewport">` 가 빠지면 모바일이 데스크톱 폭으로 렌더된다. 빌드 검사가 이를 막는다.

원본을 고쳐도 사이트는 자동으로 바뀌지 않는다. 재빌드 후 `gh-pages` 에 올려야 반영된다.


## 고지

**이 도구의 결과는 개산이다.** 실제 신고·납부는 세무 전문가의 확인을 거쳐야 한다.
동거주택 상속공제(상증법 제23조의2)는 의도적으로 구현하지 않았으므로, 요건에 해당하는 경우 세액이 실제보다 크게 나온다.


## 라이선스

Copyright © 2026 Hyojin Kim. **All rights reserved.**

소스는 **열람 목적으로만** 공개한다. 저작권자의 사전 서면 허락 없이 복제·배포·수정·상업적 이용을 할 수 없다.
전체 조건은 [LICENSE](LICENSE) 에 있다.

- **삼성생명 로고·캐릭터 이미지는 삼성생명의 자산**이며 이 라이선스가 미치지 않는다. 사용하려면 삼성생명의 별도 허락이 필요하다.
- GitHub 공개 저장소이므로 GitHub 이용약관에 따른 다른 사용자의 열람과 fork 는 제한하지 않는다.
- 있는 그대로 제공되며 어떠한 보증도 하지 않는다. 이용으로 발생하는 손해에 대해 저작권자는 책임지지 않는다.
