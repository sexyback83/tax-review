# 「AI 세무사」 에이전트 재생성 프롬프트

이 문서 하나만 주고 실행하면 같은 에이전트가 나오도록 쓴 명세다.
발표자료·팜플릿·시연영상·제출문서는 대상이 아니다 — **동작하는 에이전트와 그 검증만** 만든다.

---

## 0. 무엇을 만드나

삼성생명 FP센터 소속 FC가 **상담 자리에서** 고객의 예상 세금을 개산하는 단일 페이지 웹 도구.
8개 세목을 5단계 위저드로 순회하고, 결과에 계산 과정과 적용 기준 고지를 함께 낸다.

**프레임워크·빌드도구·서버·패키지 매니저를 쓰지 않는다.** `index.html` 을 브라우저로
열면 그대로 동작해야 한다. Node.js 는 테스트 실행에만 쓴다.

---

## 1. 절대 지켜야 할 제약

이 다섯 가지는 타협 대상이 아니다. 하나라도 어기면 다시 만들어야 한다.

### 제1조 — 계산과 화면의 분리
세액 산식과 세율·공제 상수는 **`calc.js` 에만** 둔다. `calc.js` 는 `document`·`window`·
`navigator` 를 참조하지 않고 네트워크를 쓰지 않는다. 순수 함수만 둔다.
`index.html` 은 입력 수집·단위 환산·결과 표시만 한다 — **법정 수치를 화면 코드에 두지 않는다.**
표시 문구에 금액이 필요하면 `calc.js` 의 명명 상수를 참조해 만든다
(예: `'장애인공제 = ' + W(DISABLED_DEDUCTION_PER_YEAR) + ' × ...'`).

### 제2조 — 브라우저 없이 검증 가능
계산 모듈은 `node` 로 단독 `require` 해서 테스트할 수 있어야 한다.

### 제3조 — 기대값은 법령에서 전개한다
테스트 기대값을 **구현을 실행해서 얻지 않는다.** 법 조문과 국세청 공표 세율표에서
손으로 전개하고, 단언 옆에 근거 조문을 주석으로 남긴다.

### 제4조 — 저장하지 않고 전송하지 않는다
`localStorage`·`sessionStorage`·`document.cookie`·`indexedDB`·`caches` 를 쓰지 않는다.
`fetch`·`XMLHttpRequest`·`WebSocket`·`sendBeacon`·`EventSource`·`<form>` 제출을 쓰지 않는다.
외부 스크립트·외부 폰트·외부 이미지를 불러오지 않는다(로컬 파일만).
자격증명·API 키를 코드에 두지 않는다.
**허용되는 예외 두 가지**: ① 사용자가 직접 누르는 링크(`tel:`, 카카오맵 `https://map.kakao.com/link/`,
출처 `https://kosis.kr`) ② 사용자가 직접 누르는 인쇄(`window.print()`).
위치 정보는 `navigator.geolocation` 으로 받아 **거리 계산에만 쓰고 즉시 버린다** — 저장·전송하지 않는다.

### 제7·8조 — 단순성
지금 필요한 만큼만 만든다. 중복이 실제로 두 번 이상 나타난 뒤에 공통화한다.
코드를 지우는 것이 최고의 리팩토링이다. 조기 추상화·조기 일반화를 하지 않는다.

---

## 2. 파일 구조

```
tax-review/
  index.html                 화면 전부 (인라인 CSS·JS. 약 2,900줄)
  calc.js                    세액 계산 순수 함수 (약 1,240줄)
  succession-industry.js     가업상속공제 적용대상업종 표(727개)·판정 함수·요건 데이터
  fp.js                      FP센터 데이터와 거리·투영 함수 (약 135줄)
  calc.test.js               회귀 테스트 — 구현이 설계대로인지 (131건)
  succession-industry.test.js 업종표 구조·판정 로직 회귀 테스트 (7건)
  audit.test.js              법령 대조 감사 — 설계가 법대로인지 (41건 + 범위제외 1건)
  fp.test.js                 FP센터 단언 253개
  assets/                    로컬 이미지 (로고·캐릭터)
  oracle/             독립 재구현 교차검증
    calc.py           Python 재구현 (표준 라이브러리만)
    calc.js           JS 재구현
    testcases.json    언어 중립 fixture
    calc.test.js      oracle 자체 테스트 (26건)
    fixture-check.js  fixture 대조 (43 단언)
    crosscheck-run.js JS↔Python 무작위 차분 (400케이스 2,400값)
    test_calc.py      pytest (28건)
```

`calc.js`·`succession-industry.js`·`fp.js` 는 각각 `<script src="...">` 로 **클래식
스크립트**로 싣는다(모듈 아님). 그래서 최상위 `const`·`function` 이 뒤따르는 인라인
스크립트에 그대로 보인다 — 이 점에 의존한다. 로드 순서는 `calc.js` → `succession-industry.js`
→ `fp.js` → 인라인 스크립트다. 세 파일 모두 파일 끝에서 `module.exports` 로도 내보내
node 테스트가 쓸 수 있게 한다.

`succession-industry.js` 를 `calc.js` 와 분리해 둔 이유는 성격이 다르기 때문이다 —
`calc.js` 는 세액을 계산하는 순수 함수만 담고, `succession-industry.js` 는 세액을 계산하지
않는 정성적 판정(업종코드 대조·요건 체크리스트)과 그 근거가 되는 법령 별표 데이터를 담는다.
국세청 고시로 업종표가 개정되면 이 파일의 `SUCCESSION_INDUSTRY_CODES` 배열만 고치면 되고,
`calc.js` 의 세액 계산 로직·테스트는 건드리지 않는다.

---

## 3. 화면

### 상태
```js
const STEP_START = 0;      // 첫 화면 — 세무 검토 / FP센터 두 갈래
const STEP_WIZARD_LAST = 5; // 결과
const STEP_SUMMARY = 6;    // 종합 검토 결과
const STEP_FP = 7;         // FP센터 위치
const MANWON = 10000;
const BASIS_DATE = '2026-08-19';   // 세법 기준일자
```

한 개의 `state` 객체에 모두 둔다: `step, back, customerName, sex, birth, mode, item,
vals, saved, savedMode, done, openEdit, sumEdit, fpSelected, fpNear, fpMsg, fpBusy,
resetArmed, reviewedFor`.

### 흐름
| 단계 | 화면 | 내용 |
|---|---|---|
| 0 | 첫 화면 | 좌우 반씩 두 카드 — 「세무 검토」 / 「FP센터」. 하단 여백에 캐릭터 이미지 |
| 1 | 고객 정보 | 고객명 · 성별(남/여 버튼) · 생년월일(YYYYMMDD) → 만 나이 자동 표시 |
| 2 | 검토 모드 | 「간편」(필수값만, 나머지는 법정 기본 가정) / 「심화」(세부 옵션 전부) |
| 3 | 세목 선택 | 8개 카드. 검토 완료 항목에 표시 |
| 4 | 값 입력 | 세목별 필드. 만원 단위. 입력 중 안내 문구가 실시간으로 바뀐다 |
| 5 | 결과 | 큰 세액 + 계산 과정 + 시나리오 표 + 적용 기준 고지 + 다음 세목 |
| 6 | 종합 결과 | 검토한 항목의 결과·입력값·가정을 한 화면에. 입력값 수정 → 즉시 재계산 |
| 7 | FP센터 | 손으로 단순화한 한국 지도(인라인 SVG) + 8곳 목록 + 「가까운 센터 찾기」 |

### 상단 버튼 (STEP 1 이후 항상)
`처음으로` · `입력값 초기화` · `검토결과 확인 N` · `FP센터 위치`

- `입력값 초기화` 는 **두 번 눌러야** 지운다(첫 클릭은 `초기화 확인` 으로 바뀜). 새 고객 응대 시 개인정보 잔존 방지.
- `검토결과 확인` 은 검토 완료 항목이 1개 이상일 때만 활성.
- 좁은 화면에서는 짧은 라벨로 바꾼다(`초기화`, `검토 결과`, `FP센터`).

### 지켜야 할 화면 불변조건
1. **헤더 높이가 모든 화면에서 같아야 한다.** 버튼이 없는 화면에서도 자리를 비워 둔다
   (`display:none` 대신 `visibility:hidden`, `min-height` 지정).
2. **스크롤바가 생겨도 로고·제목이 움직이지 않아야 한다** — `html { scrollbar-gutter: stable }`.
3. STEP 표시와 빵가루는 **한 줄에** 두고, 좁은 화면에서 줄바꿈하지 않는다(`flex-wrap:nowrap; overflow-x:auto`).
4. 입력 중에는 **재렌더하지 않는다.** 포커스와 커서 위치가 날아간다.
   `[data-hint]`·`[data-derived]`·`[data-opts]` 만 갱신하는 `refreshDerived()` 를 쓴다.
5. 금액 표시는 전부 `formatWon()` 을 지난다. **숫자가 아니면 `—` 를 반환**해 `NaN만원` 을 그리지 않는다.
6. 고객 정보가 바뀌었는데 앞 고객의 검토 결과가 남아 있으면 **경고를 띄운다**.
   자동으로 지우지 않는다 — 오타 수정으로 입력을 날리면 더 나쁘다.
7. 결과 진입 게이트는 **세목이 스스로 선언**한다(`ready(g)`). 선언이 없으면 첫 만원 필드가
   0보다 큰지만 본다. **상속세는 반드시 `ready` 를 선언해 부동산+금융자산+기타 합계로 판정한다** —
   첫 필드(부동산)만 보면 현금·예금만 있는 고객이 막히고, 금융자산을 부동산 칸에 넣어 우회하면
   금융재산 상속공제(한도 2억원)가 사라져 세액이 과대 계상된다.
8. 종합 결과에 **인쇄 · PDF 저장** 버튼을 둔다. `@media print` 로 조작 요소를 숨기고
   카드가 페이지 사이에서 잘리지 않게 하며, 인쇄물에 개산 고지를 자동으로 붙인다.

### 세목 정의 형태
```js
const ITEMS = [{
  key: 'inherit', name: '상속세', desc: '일괄/인적공제 자동 유리선택 · 1·2차 상속 합계',
  fields: [ { k: 're', label: '부동산', unit: '만원' }, ... ],
  ready(g) { ... },              // 선택 — 결과 진입 조건
  run(g) { ... },                // calc.js 를 불러 { total, label, sub, steps, tables, notes } 반환
}, ...];
```
`accessor()` 가 `{ num, won, raw, pick }` 을 준다. **간편 모드에서는 심화 전용(`adv: true`)
필드를 법정 기본 가정값으로 대체한다.** 그래서 같은 항목을 간편/심화로 검토하면 값이 달라질 수
있어, 종합 결과가 검토 당시와 같은 숫자를 보이도록 `savedMode` 에 모드를 함께 보관한다.

---

## 4. 8개 세목과 필드

만원 단위 입력, `심화` 는 심화 모드 전용, `def` 는 기본값.

**1. 상속세** (`inherit`) — 필드 16
`deathDate`상속개시일(심화·date) · `re`부동산 · `cash`금융자산(현금) · `etc`기타자산 ·
`debt`채무 합계 · `finDebt`그중 금융기관 채무 · `spouse`배우자(choice 있음/없음, def 있음) ·
`children`자녀 수(명, def 2) · `spouseActual`배우자 실제 상속액(심화) · `spouseAsset`배우자 고유재산 ·
`spouseFin`그중 금융자산(심화) · `funeral`장례비용(심화) · `pregift`사전증여재산(심화) ·
`skipShare`손자녀 등 세대생략 상속분(심화) · `skipMinor`세대생략 상속인 미성년자(심화·choice) ·
`elders`65세 이상 부양가족 수(심화·명)

**2. 증여세 (부담부증여)** (`gift`) — 필드 10
`value`증여재산가액 · `debt`수증자 인수 채무액 · `rel`증여자와의 관계(choice, def 직계존비속) ·
`relDed`증여재산공제 적용(choice, def 적용) · `acquire`증여재산 취득가액 · `skip`수증자가 손자녀 등(choice) ·
`prior`10년 내 사전증여 합계(심화) · `giftDate`증여일(심화) · `doneeBirth`수증자 생년월일(심화) ·
`years`증여자 보유기간(심화·년, def 10)

**3. 종합부동산세** (`jongbu`) — 필드 10
`pub`주택 공시가격 합계 · `houses`주택 수(채, def 1) · `single`1세대1주택 단독명의(readonly) ·
`hold`보유기간(심화·년, def 5) · `ownerAge`만 나이(심화) · `ptax`기납부 재산세 중복분(심화) ·
`resident`해당 주택 거주(심화·choice, def 예) · `liveYears`거주기간(심화) ·
`residentRatioShown`거주비중(심화·readonly — 거주 주택수 ÷ 전체 주택수) · `adjusted`조정대상지역(심화·choice)

> 개편안 다주택 기본공제의 거주비중은 **주택 수** 기준이라 거주주택 공시가격을 받을 필요가 없다.
> 예전에는 `residentPub`(거주주택 공시가격)을 받아 공시가격 비중으로 계산했는데, 개편안 자료의
> 「③ 적용 사례」가 2주택 1채 거주 = 4억 + (5억 × **1/2**), 3주택 1채 거주 = 4억 + (5억 × **1/3**)로
> 못박아 그 필드를 지웠다. 채마다 가격이 같으면 두 방식이 우연히 같은 값을 내므로,
> **가격이 다른 사례**로 회귀 테스트를 걸어 둔다 (`calc.test.js` JR12).

**4. 양도소득세** (`transfer`) — 필드 7
`sale`양도가액 · `buy`취득가액 · `years`보유기간(년, def 10) · `exempt`1세대1주택 비과세(choice) ·
`live`거주기간(년) · `cost`필요경비(심화) · `surcharge`다주택 중과(심화·choice)

**5. 비상장주식 평가** (`stock`) — 필드 9
`netAsset`순자산가액 · `inc1/inc2/inc3`순손익액 1·2·3년 전 · `weighted`가중평균 순손익액(readonly) ·
`shares`발행주식 총수(주, def 10000) · `netOnly`순자산가치만 평가(심화·choice) ·
`realty`부동산 과다보유 법인(심화·choice) · `premium`최대주주 할증(심화·choice, def 20% 할증)

**6. 급여 및 배당** (`paydiv`) — 필드 4
`salary`현재 급여액(연간) · `dividend`예상 배당액(연간) · `step`급여 증감 비교 폭(심화·%, def 10) ·
`insur`4대보험 반영(심화·choice, def 예)

**7. 임원퇴직금** (`retire`) — 필드 7
`avgPay`최종 3년 연평균 급여 · `avgPay2019`2019.12.31. 기준 3년 평균급여(심화) · `joinDate`입사일(date) ·
`retireDate`퇴직(예정)일(date) · `years`근속연수(readonly) · `mult`정관상 지급배수(배, def 2) ·
`otherInc`퇴직 연도의 다른 근로소득(심화)

**8. 가업승계제도 검토** (`succession`) — 필드 8
`biz`법인 주식 검토 가액 · `unrelated`사업무관자산 비율(%) · `years`피상속인(증여자) 경영기간(년, def 15) ·
`route`검토 경로(choice, def 가업상속공제) · `landUnit`㎡당 토지 평가액(심화) · `landArea`토지 면적(심화·㎡) ·
`floorArea`건축물 바닥면적(심화·㎡) · `metro`소재지(심화·choice, def 수도권)

**8번 필드 입력 화면 상단에는 「가업상속공제 사전 확인」 패널이 붙는다.** 세액을 계산하지 않는
정성적 체크리스트라 `item.fields`·`run()`과는 분리해 두었다 — `renderStep4()`가
`state.item === 'succession'` 일 때만 `successionCheckHtml()`을 끼워 넣는다.
- 사업자등록증 업종코드를 입력하면 `succession-industry.js`의 `checkSuccessionIndustry()`가
  727개 적용대상 업종표(`SUCCESSION_INDUSTRY_CODES`, 상증법 시행령 제15조 [별표])와 대조해
  즉시 결과를 보여준다.
  정확히 일치하지 않으면 국세청 업종코드(6자리)가 표준산업분류 코드를 앞자리로 포함한다고 보고
  가장 긴 접두 일치를 참고로 보여주며(`approximate: true`), 그 사실을 화면에 함께 고지한다.
- 피상속인 요건 3개·상속인 요건 4개(`SUCCESSION_DECEDENT_REQUIREMENTS`·`SUCCESSION_HEIR_REQUIREMENTS`,
  상증법 제18조의2 제1항·시행령 제15조 제3항)를 체크박스로 두고, 업종·피상속인·상속인 상태를
  한 줄로 요약한다.
- 입력값은 다른 필드와 같이 `state.vals`(키 `succCode`·`succ_dec_*`·`succ_heir_*`)에 담겨
  `saveCurrent()`·`resetAll()`을 그대로 탄다 — 새 저장소를 만들지 않는다(제7·8조).
- 이 패널의 판정은 참고용이다. 세액 계산 결과의 「적용 기준 고지」에도 "업종 판정은 사전
  확인란에서 별도로 안내한다"는 문구로 반영한다.

**임원퇴직금은 `ready(g)` 를 선언한다** — 연평균급여가 있어야 하고, 입사일·퇴직일에서 근속연수가
산출되어야 한다. 근속기간은 2011년 이전 / 2012~2019 / 2020년 이후 세 구간으로 월 단위로 나눠 배분한다.

---

## 5. `calc.js` 상수 — 근거 법령과 함께

각 상수 옆에 조문을 주석으로 남긴다. `억 = 100000000`, `만 = 10000` 을 두고 상수를 사람이 읽는 꼴로 쓴다.

**상속세** (상속세 및 증여세법)
```
일괄공제 5억 · 기초공제 2억 · 자녀 1인당 5,000만 · 미성년자 1년당 1,000만
연로자(65세 이상) 1인당 5,000만 · 장애인 기대여명 1년당 1,000만 (제20조 제1항)
배우자상속공제 하한 5억 / 상한 30억 (제19조) · 미성년자 기준 19세 미만 (제20조 제1항 제2호)
장례비 인정 한도 1,000만 · 금융재산공제 하한 2,000만 / 상한 2억 (제22조)
신고세액공제 3% (제69조) · 세대생략 할증 30%, 미성년자 20억 초과 40% (제27조)
세율 5단계 누진: 1억 10% / 5억 20% / 10억 30% / 30억 40% / 초과 50%
기대여명은 통계청 완전생명표(1세별) 2024년 기준을 성별·연령별 배열로 내장
```

**증여세** — 배우자 6억 · 직계존비속 5,000만(미성년 2,000만) · 기타친족 1,000만
**양도소득세** — 기본공제 250만 · 지방소득세 10% · 1세대1주택 비과세 기준 12억 ·
장기보유특별공제 표1(1년당 2%, 한도 30%) / 표2(보유·거주 각 1년당 4%, 합계 한도 80%, 각 40%, 거주 최소 2년) ·
다주택 중과 2주택 20% / 3주택 30% · 10년 이상 장기거주 기본공제 2,500만(12억 초과 30억 이하)
**종합부동산세** — 1세대1주택 12억 / 일반 9억 · 공정시장가액비율 60% ·
고령자+장기보유 합계 한도 80% · 농어촌특별세 20% · 세부담상한 150% (제10조·제15조)
**2026 개편안 기본공제** — 1세대1주택자 특례 거주 14억 / **비거주 12억** ·
다주택 4억 + 5억 × (**거주 주택수 ÷ 전체 주택수**). 공시가격 비중이 아니다.
**세부담상한은 개편안도 150%** — 당초 정부안 200%에서 수정돼 현행과 같아졌다.
이 도구는 직전연도 재산세·종부세를 입력받지 않아 상한 적용을 계산하지 않는다.
현행 세율 2주택 이하 0.5~2.7% / 3주택 이상 0.5~5.0% (제9조 제1항)
**2026 개편안 §(5) 세율 일원화** (종부법 §9①·②) — 구간 경계는 현행과 같고 세율만 바뀐다.
`'27년` 2주택 이하 0.5/0.7/**1.3**/1.5/2.0/2.7/**3.5**% · 3주택 이상 0.5/0.7/**1.3**/2.0/3.0/4.0/5.0%
`'28년 이후` 주택수 차등 폐지 — 가액 기준 단일표 0.5/0.7/1.3/2.0/3.0/4.0/5.0%
`'27년 3주택 이상`과 `'28년 단일표`는 수치가 같지만 뜻이 다르므로 상수를 따로 둔다.
**2026 개편안 §(6) 세액공제 전환** (종부법 §9⑤·⑧·⑨) — 보유공제 → 거주공제.
거주공제는 현행 보유공제와 같은 표(5년 20% / 10년 40% / 15년 50%)이고 보유공제는 그 **1/2**이다.
`'27년` 둘 중 **높은 공제율** · `'28년 이후` **거주공제만**. 연령공제·합계 한도 80%는 좌동.
금액 한도 신설 `'27년` 800만원 → `'28년 이후` 600만원.
**공정시장가액비율 차등은 세율과 별개로 남는다** — `'28년`에도 3주택 이상·조정대상지역 80%.
**비상장주식** — 환원율 10% · 순자산가치 하한 80% · 최대주주 20% 할증 ·
순손익 가중치 3:2:1 (직전·전전·전전전, 합 6) (상증령 제54조)
**급여·배당** — 금융소득종합과세 기준 2,000만 (소득세법 제14조 제3항) ·
원천징수 15.4%(14% + 지방 1.4%) · 본인 기본공제 150만 (제50조) · 근로소득공제 한도 2,000만 (제47조) ·
4대보험 근로자 부담 약 9%
**임원퇴직금** — 지급배수 한도 2012~2019 3배 / 2020 이후 2배 (소득세법 제22조 제3항).
2011년 이전 근무분은 한도 적용 제외. 3배 구간 한도는 **2019.12.31. 기준 3년 평균급여**로 계산한다
(최종 급여가 아니다 — 미입력 시 최종 3년 평균으로 갈음하고 그 사실을 화면에 고지)
**가업승계** — 공제 한도 경영 10년 300억 / 20년 400억 / 30년 600억 ·
증여세 과세특례 공제 10억, 10% (120억 초과분 20%)
**2026 세제개편안** — 현행/개편안을 `BASIS_CURRENT`·`BASIS_REFORM_2026` 로 선택 가능하게 하고
적용 연도(2027·2028·2029)별 값을 분리한다. 개편안에서만 한도가 생기는 항목은 **현행에서 `Infinity`**
로 둔다(「한도 없음」의 의도된 표현).

---

## 5-1. `succession-industry.js`

`calc.js` 와 분리된 별도 파일이다 — 세액을 계산하지 않고, 국세청 고시로 자주 개정될 수 있는
법령 별표 데이터를 담기 때문이다(제7·8조: 성격이 다른 데이터는 다른 파일에 둔다).

```js
const SUCCESSION_INDUSTRY_CATEGORIES = { A: '농업, 임업 및 어업', ..., S: '협회 및 단체, 수리 및 기타 개인 서비스업' };
const SUCCESSION_INDUSTRY_CODES = [
  ['A', '1123', '종자 및 묘목 생산업', '가업용 자산 중 토지·건물의 가액이 ... 100분의 50 미만인 경우만 해당'],
  ['B', '5100', '석탄 광업'],
  // ... 상증법 시행령 제15조 제1항 [별표] 전체 727개
];
```

근거: 상속세 및 증여세법 제18조의2 제1항 제2호, 같은 법 시행령 제15조 제1항 [별표]
(「가업상속공제 적용 대상 업종」, 한국표준산업분류 기준). FC가 보유한 별표 원문(엑셀 표기)을
그대로 전사했으며, 727은 별표 전체의 세세분류 개수다. 종자·묘목 생산업(1123)은 토지·건물가액
비율 50% 미만 조건이, 음식점업(56111~56199)은 "직접 제조·조리하지 않으면 제외" 단서가
붙는다 — 둘 다 배열의 네 번째 칸(비고)에 그대로 남겨 화면에 보여준다.

함수: `checkSuccessionIndustry(inputCode)` — 숫자만 남긴 뒤 정확히 일치하는 코드를 찾고,
없으면 가장 긴 접두 일치를 참고로 반환한다(`approximate: true`). 사업자등록증에는 표준산업분류
(4~5자리)가 아니라 국세청 업종코드(6자리)가 적히는 경우가 많고, 그 코드가 표준산업분류
코드를 앞자리로 포함하는 경우가 많다는 실무 관행을 반영한 규칙이다.

데이터: `SUCCESSION_DECEDENT_REQUIREMENTS`(피상속인 요건 3개)·`SUCCESSION_HEIR_REQUIREMENTS`
(상속인 요건 4개) — 상증법 제18조의2 제1항, 같은 법 시행령 제15조 제3항. 각 항목은
`{ id, label, basis }` 형태이며 예/아니오로만 판단하는 체크리스트라 세액처럼 계산하지 않는다.

**유지보수**: 국세청 고시로 적용대상 업종이 바뀌면 이 파일의 `SUCCESSION_INDUSTRY_CODES` 배열에
`[대분류 문자, 업종코드, 업종명, 비고]` 행을 추가·삭제하면 된다. `calc.js`의 세액 계산 로직·
`index.html`의 다른 세목 화면은 건드릴 필요가 없다.

---

## 6. `fp.js`

```js
const FP_SOURCE = { 심의번호: '25-3281', 담당: 'WM팀',
                    유효기간: { 시작: '2025-10-28', 만료: '2026-10-27' } };
```
센터 8곳(이름·전화·주소·lat·lng)을 인쇄물 표기 그대로 둔다.
서울 37.570962/126.981438 · 강남 37.496744/127.025704 · 경인 37.450259/126.705064 ·
경원 37.275631/127.030594 · 충청 36.358174/127.382504 · 부산 35.149733/129.058779 ·
대구 35.866318/128.592799 · 호남 35.153292/126.910021

함수: `fpExpiryStatus(오늘)`(만료 60일 전부터 경고) · `distanceKm()`(하버사인, R=6371) ·
`nearestCenters()`(입력 배열 불변) · `inKorea()`(위 33~39, 경 124~132) ·
`mapSize()`·`projectPoint()`(정거원통도법, 실제 거리비로 가로세로 유지, 북쪽이 위).

**지도는 인라인 SVG 로 직접 그린다.** 카카오맵·구글맵 SDK 는 JavaScript 키를 HTML 에 박아야 하므로
제4조와 충돌한다. 한국 외곽선을 47개 좌표로 단순화해 내장하고, 제주는 실제 동서 73km·남북 31km
비율로 타원으로 그린다. 각 센터는 키 없는 `https://map.kakao.com/link/map/이름,위도,경도` 로 연결한다
(사용자가 누르는 이동이므로 데이터 전송이 아니다).

---

## 7. 테스트 — 네 겹으로 만든다

| 파일 | 역할 | 기대값의 출처 |
|---|---|---|
| `calc.test.js` | 회귀 — 구현이 설계대로인가 | 법정 산식에서 손으로 전개 |
| `audit.test.js` | 감사 — **설계가 법대로인가** | 법 조문과 국세청 세율표에서만. 구현·기존 테스트·문서를 참조하지 않는다 |
| `oracle/` | 교차검증 — JS·Python 독립 재구현이 같은 값을 내는가 | 언어 중립 fixture |
| `fp.test.js` | FP센터 데이터·거리·유효기간 | 하버사인 손계산 |
| `succession-industry.test.js` | 가업상속공제 적용대상업종 표·판정 로직 회귀 | 별표 원문 개수(727)·코드 중복 없음을 눈으로 대조 |

의존성 없이 `node <파일>` 로 실행되게 만든다(테스트 프레임워크 없음). pytest 만 예외.

**`audit.test.js` 의 설계** — 소유자가 구현하지 않기로 결정한 항목은 `excludedByOwner()` 로
따로 센다. 그 항목이 **통과하기 시작하면 결함으로 보고**한다(제외 목록이 낡는 것을 막는다).
출력은 `감사 결과 — 통과 N건 / 범위 제외 N건 / 결함 N건`.

**`fp.test.js` 의 드리프트 검사** — FP센터 데이터 사본이 두 벌 존재하면(통합본·독립 페이지)
전 필드·심의번호·만료일·거리 64조합·유효기간 4경계를 대조한다. 준법 데이터가 한쪽만
고쳐지는 것이 실제 위험이다.

**`calc.js` 결과 검증** — 공개 계산 함수를 감싸 반환 객체에 **NaN 이 있으면 던진다**.
필수 키를 함수마다 열거하는 대신 결과를 본다. **`Infinity` 는 거르지 않는다** — 「한도 없음」을
뜻하는 의도된 값이다(이걸 거르면 종부세·양도세 테스트가 무더기로 깨진다).

### 통과 기준
```
node calc.test.js               → PASS 131 / FAIL 0
node succession-industry.test.js→ PASS 7 / FAIL 0
node fp.test.js                 → 253개 단언 통과
node audit.test.js              → 통과 41건 / 범위 제외 1건 / 결함 0건
node oracle/calc.test.js        → 26건 PASS
node oracle/fixture-check.js    → 43 passed, 0 failed
node oracle/crosscheck-run.js   → 400 cases, 2,400 values, 0 mismatched
python -m pytest oracle/test_calc.py → 28 passed
```
`succession-industry.test.js`는 세액을 계산하지 않는 판정 로직이라 oracle 교차검증·감사
(`audit.test.js`) 대상에는 넣지 않았다. 대신 별표 원문 전사가 맞는지는 총 개수(727)와
코드 중복 없음으로 구조적으로 검증한다.

---

## 8. 자기검증 벡터 — 이 값이 나오면 맞다

전부 실제 구현을 실행해 얻은 값이다. 금액은 `formatWon()` 표기.

**V1 상속세** — 부동산 20억 · 금융자산 10억 · 배우자 있음 · 자녀 2 · 상속개시 2026-08-19
```
totalEstate 30억 / taxBase 10억 1,429만원 / calculatedTax 2억 4,571만원 / finalTax 2억 3,834만원
일괄공제 5억 / 배우자상속공제 12억 8,571만원 / 금융재산공제 2억 / 법정상속분 12억 8,571만원
```

**V2 2차 상속** — V1 결과 기준, 배우자 고유재산 0, 자녀 2
```
배우자 취득 재산 12억 8,571만원 / 2차 세액 1억 7,044만원 / 1·2차 합계 4억 879만원
```
> 2차 상속재산에 더할 금액은 **배우자상속공제액이 아니라 배우자가 실제로 취득하는 재산**이다.
> 공제액은 하한 5억·상한 30억·법정상속분에 걸리므로 취득액과 어긋난다. 공제액을 쓰면 양방향으로 틀린다.

**V3 상속세** — 부동산 0 · 금융자산 30억 (금융재산공제 상한 확인)
```
taxBase 10억 1,429만원 / finalTax 2억 3,834만원 / 금융재산공제 2억
```
> 같은 30억이라도 전액 부동산이면 과세표준 12억 1,429만원 · 세액 3억 1,594만원이다.
> 차이 7,760만원이 금융재산 상속공제다. 화면이 부동산 입력을 강제하면 이 공제가 사라진다.

**V4 증여세(부담부증여)** — 증여재산 10억 · 채무인수 3억 · 직계존비속 · 취득가액 4억 · 보유 10년
```
증여분 7억 / 증여재산공제 5,000만원 / 과세표준 6억 5,000만원 / 증여세 1억 3,095만원
양도분 취득가액 1억 2,000만원 / 양도차익 1억 8,000만원 / 양도세 3,749만원 / 합계 1억 6,844만원
```

**V5 종합부동산세** — 1세대1주택 공시 20억 · 보유 5년 · 현행
```
기본공제 12억 / 공정시장가액비율 0.6 / 과세표준 4억 8,000만원 / 산출세액 276만원
장기보유 공제율 0.2 / 공제액 55만원 / 결정세액 221만원 / 농특세 44만원 / finalTax 265만원
```

**V6 양도소득세** — 양도 10억 · 취득 5억 · 보유 10년 · 1세대1주택 아님 · 현행
```
양도차익 5억 / 장기보유공제율 0.2 → 1억 / 기본공제 250만원 / 과세표준 3억 9,750만원
산출세액 1억 3,306만원 / 지방소득세 1,331만원 / finalTax 1억 4,637만원
```

**V7 비상장주식 평가** — 순자산 50억 · 순손익 10/8/6억 · 10,000주 · 최대주주 할증
```
가중평균 순손익액 8억 6,667만원 / 순자산가치 50만원 · 순손익가치 87만원
가중평균 72만원 / 하한(순자산 80%) 40만원 / 주당 72만원 → 할증 1.2 → 86만원 / 총액 86억 4,000만원
```

**V8 급여·배당** — 급여 1.2억 · 배당 3,000만원 · 4대보험 반영
```
총 세부담 2,919만원 / 세후 수령액 1억 1,001만원
배당 분리과세분 308만원(2,000만원 × 15.4%) / 초과분 385만원
```

**V9 임원퇴직금** — 연평균급여 1억 · 근속 21년(2005-03-01 ~ 2026-03-01) · 배수 2
```
지급액(세전) 4억 2,000만원 / 한도 5억원 / 초과 0 / 퇴직소득세 3,886만원
근속 21년이 7 / 8 / 6년으로 갈린다 (경계일 2012-01-01 · 2020-01-01)
한도 = 1.4억(2011년 이전, 한도 적용 제외) + 2.4억(8년 × 3배) + 1.2억(6년 × 2배)
```
> 화면에서 이 값을 「수령액」으로 쓰지 않는다 — 급여·배당 항목의 「세후 수령액」과 뜻이 어긋난다.
> **「세전 지급액」**으로 표기한다.
>
> **한도는 지급액과 다르다.** 지급액은 근속연수 전체에 정관상 배수를 곱한 값이고,
> 한도는 소득세법 제22조 제3항의 세 구간으로 갈라 각각 다른 배수로 계산한 합이다.
> 둘이 우연히 같아 보이면 구간 배분이 빠진 것이다 — 여기서는 4.2억 < 5억이라 초과가 0이다.

**V10 가업승계** — 검토가액 100억 · 사업무관 20% · 경영 15년 · 가업상속공제 · 현행
```
사업무관자산 20억 / 가업해당 80억 / 공제 한도 300억 / 공제액 80억
```

**V11 가업상속공제 적용대상업종** — `checkSuccessionIndustry()`
```
'62010' → 정보통신업 · 컴퓨터 프로그래밍 서비스업 (정확히 일치, note 없음)
'620100' → 위와 동일 업종을 접두 일치로 참고 표시 (approximate: true)
'56111' → 숙박 및 음식점업 · 한식 일반 음식점업 (note: 직접 제조·조리하지 않으면 제외)
'99999' → 목록에 없음 (matched: false)
```
> 사업자등록증에는 표준산업분류(4~5자리)가 아니라 국세청 업종코드(6자리)가 적히는 경우가
> 많아, 정확히 일치하지 않으면 가장 긴 접두 일치를 참고로 보여준다. 정확한 판정은 별표 원문
> 코드(표준산업분류)로 재확인해야 한다는 점을 화면에 함께 고지한다.

**FP센터** — 강남역(37.4979, 127.0276) 기준 가까운 순
```
강남 0.21km / 서울 9.09km / 경원 24.72km
위도 1도 = 111.195km / 서울↔부산 327.20km
```

---

## 9. 만들면서 실제로 틀렸던 것들 — 같은 함정을 피하라

1. **2차 상속의 기준** — 배우자상속공제액을 이전 재산으로 쓰면 틀린다. V2 주석 참고.
   100억 사례에서 5억 7,507만원 과소계상이 났다.
2. **`<a:ea>` 없이 폰트를 바꿈** — (문서용) OOXML 한글은 `latin` 이 아니라 `ea` 로 렌더된다.
3. **`vh` 단위** — 프레임 높이가 자동인 환경에서 `vh`·백분율 높이는 0으로 붕괴한다. 절대 px 로.
4. **`querySelector('.cta')`** — 「이전」 버튼도 같은 클래스를 쓴다. `[data-act=next]` 로 집어야 한다.
5. **로고 여백** — 이미지 좌우에 투명 여백이 있으면 `object-fit: cover` 로 잘라 쓴다.
   승인받은 파일을 재인코딩하지 않는다.
6. **결과 검증에 `Number.isFinite`** — `Infinity`(한도 없음)를 걸러 테스트 18건이 깨졌다. NaN 만 본다.
7. **첫 필드만 보는 게이트** — 상속세에서 현금만 있는 고객을 막는다. 합계로 판정한다.

---

## 10. 완료 조건

1. `index.html` 을 브라우저로 열어 8개 세목을 전부 검토할 수 있다.
2. §7의 7개 명령이 적힌 숫자 그대로 나온다.
3. §8의 벡터가 전부 일치한다.
4. `src` 전체 grep 으로 제4조 금지 패턴이 **0건**이다
   (`localStorage|sessionStorage|document.cookie|indexedDB|fetch\(|XMLHttpRequest|WebSocket|api_key|secret|token`).
5. `index.html` 에 `숫자 * MANWON` 형태의 법정 수치 연산이 **0건**이다(제1조).
6. 콘솔 오류가 0건이다.
